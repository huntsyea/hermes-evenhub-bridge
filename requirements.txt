# Runtime dependencies for the hermes-evenhub-bridge plugin.
# Mirrors [project.dependencies] in the source repo's bridge/pyproject.toml.
# Install into the same environment that runs Hermes, e.g.:
#   ~/.hermes/hermes-agent/venv/bin/pip install -r requirements.txt
faster-whisper>=1.2.1
numpy>=1.26.0
websockets>=16.0
